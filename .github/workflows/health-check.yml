name: Daily Health Check

on:
  schedule:
    # Run daily at 12:15 PM Finnish time (9:15 UTC during summer, 10:15 UTC during winter)
    - cron: '15 9 * * *'
  workflow_dispatch: # Allow manual triggering

jobs:
  health_check:
    runs-on: ubuntu-latest
    steps:
      - name: Check the deployed service URL
        uses: jtalk/url-health-check-action@v4
        with:
          url: ${{ secrets.RENDER_APP_URL }}
          follow-redirect: true
          max-attempts: 3
          retry-delay: 5s
          retry-all: false

      - name: Discord notification - Health check passed
        if: success()
        run: |
          curl -X POST -H "Content-Type: application/json" \
            -d "{\"content\": \"✅ **Daily Health Check Passed!** Your Pokédex app is running smoothly.\"}" \
            ${{ secrets.DISCORD_WEBHOOK }}

      - name: Discord notification - Health check failed
        if: failure()
        run: |
          curl -X POST -H "Content-Type: application/json" \
            -d "{\"content\": \"🚨 **ALERT: Daily Health Check Failed!** Your Pokédex app might be down. Please investigate immediately!\"}" \
            ${{ secrets.DISCORD_WEBHOOK }}
